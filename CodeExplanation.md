# คำอธิบายโค้ด (แบบสั้น) — โปรไฟล์ / ตั้งค่า / รางวัล / วิดเจ็ต

สรุปว่าแต่ละฟังก์ชันในโค้ดของ 4 ฟีเจอร์นี้ "ทำหน้าที่อะไร" แบบสั้นๆ เข้าใจง่าย สำหรับใช้ตอบคำถามอาจารย์ (ไม่ลงรายละเอียดเทคนิคลึก)

---

## 1. โปรไฟล์ผู้ใช้ (Profile)

**ไฟล์หลัก**: `src/pages/Profile.jsx`

- **`loadProfileData()`** (ทำงานอัตโนมัติตอนเปิดหน้า) — โหลดโพสต์ของตัวเอง, แบบร่าง, บุ๊กมาร์ก และจำนวนผู้ติดตาม พร้อมกันทีเดียวตอนเข้าหน้าโปรไฟล์
- **`useEffect` (wallpaper theming)** — เรียก `setHeroImage()` จาก `heroThemeStore` เพื่อวิเคราะห์ความสว่างของรูปพื้นหลังที่ผู้ใช้ตั้งไว้ แล้วปรับให้ navbar/การ์ดเปลี่ยนเป็นโทนมืด-สว่างอัตโนมัติให้เข้ากับภาพ
- **`getGlassColor()` / `rgbToRgba()`** (`utils/colorUtils.js`) — คำนวณสีของการ์ดกระจกฝ้าบนหน้าโปรไฟล์ให้กลืนไปกับโทนสีของพื้นหลัง
- **ส่วนแสดงผล (avatar + กรอบ + แบนเนอร์)** — เอา `avatar_url`, `banner_url`, `profile_frame_id` จากข้อมูลผู้ใช้มาวาดเป็นรูปโปรไฟล์ วงกลม/สี่เหลี่ยมตามที่ตั้งค่าไว้
- **แท็บ (โพสต์ / แบบร่าง / บุ๊กมาร์ก / ความสำเร็จ)** — สลับแสดงเนื้อหาแต่ละหมวดด้วย state `activeTab` ตัวเดียว ไม่ต้องโหลดข้อมูลใหม่ทุกครั้งที่สลับแท็บ

---

## 2. ระบบตั้งค่า (Settings)

### `src/pages/settings/SettingsOverview.jsx` (หน้ารวม)
- ดึงสถิติภาพรวม (`profileService.getStats()`) มาโชว์เป็นการ์ดตัวเลข พร้อมลิงก์ทางลัดไปหน้าตั้งค่าย่อยแต่ละหน้า

### `src/pages/settings/SettingsProfile.jsx` (ตั้งค่าโปรไฟล์)
- **`handleFileChange(e, type)`** — ตอนผู้ใช้เลือกไฟล์รูป (avatar/วอลเปเปอร์/แบนเนอร์) เช็คขนาด+ชนิดไฟล์ก่อน แล้วสร้างรูปตัวอย่างชั่วคราวในเครื่อง
- **`updateTheme(key, value)`** — แก้ค่าธีมทีละ field เช่นตอนเลือกรูปทรง avatar
- **`addTag()` / `removeTag(index)`** — เพิ่ม/ลบแท็กความสนใจของผู้ใช้ (จำกัดสูงสุด 5 แท็ก)
- **`handleEquipFrame(frameId)`** — กดเลือกกรอบรูปแล้วบันทึกทันที ไม่ต้องรอกดปุ่ม "บันทึกข้อมูล" หลัก
- **`handleEquipWallpaper(url)`** — เหมือนกันแต่ใช้ตอนเลือกวอลเปเปอร์จากของรางวัลที่ปลดล็อกแล้ว
- **`handleSubmit(e)`** — รวบรวมข้อมูลทั้งฟอร์ม (ชื่อ, bio, ไฟล์รูปที่เลือกไว้ ฯลฯ) แล้วส่งไปบันทึกที่เดียวตอนกด "บันทึกข้อมูล"

### `src/pages/settings/SettingsAppearance.jsx` (ตั้งค่ารูปลักษณ์การ์ด)
- **`updateTheme(key, value)`** — แก้ค่าธีม (เค้าโครง/สไตล์/สี/ความโค้งมน/สีตัวอักษร) ทีละ field เหมือนกับหน้าโปรไฟล์แต่คนละกลุ่มฟิลด์
- **`handleSubmit(e)`** — บันทึกธีมทั้งหมดไปที่ backend

### `src/pages/settings/SettingsAccount.jsx` (ตั้งค่าบัญชี)
- **`handleSaveEmail(e)`** — ส่งคำขอเปลี่ยนอีเมล (ระบบจะส่งลิงก์ไปยืนยันที่อีเมลใหม่)
- **`handleSavePassword(e)`** — เช็กว่ารหัสผ่านยาวพอและกรอกยืนยันตรงกัน ก่อนส่งเปลี่ยนรหัสผ่าน
- **`toggleNotificationPref(key)` / `handleSavePrefs()`** — เปิด/ปิดและบันทึกว่าจะรับการแจ้งเตือนประเภทไหนบ้าง (ไลก์/คอมเมนต์/ระบบ)

### `src/components/settings/ProfilePreview.jsx` (พรีวิวสด)
- ไม่มีฟังก์ชันโต้ตอบ แค่รับค่าฟอร์มปัจจุบันมาวาดเป็นภาพจำลองมือถือ ให้เห็นผลลัพธ์ทันทีก่อนกดบันทึกจริง (ใช้ร่วมกันทั้งหน้า Profile และ Appearance settings)

### `src/components/settings/FrameDecorationModal.jsx` (ป๊อปอัปเลือกกรอบ)
- แสดงกรอบทั้งหมดที่มีในระบบรางวัล กรอบที่ยังไม่ปลดล็อกจะจางและกดไม่ได้ ผู้ใช้เลือกแล้วกด "ยืนยัน" ค่อยส่งกลับไปให้ `handleEquipFrame` บันทึกจริง

---

## 3. ระบบรางวัล / ภารกิจ (Achievements)

**ไฟล์หลัก**: `src/store/achievementStore.js` (เก็บข้อมูลกลาง) + `src/pages/Achievements.jsx` (หน้าแสดงผล)

- **`fetchMilestones()`** — ดึงรายการภารกิจ/รางวัลทั้งหมดของผู้ใช้จาก backend
- **`claimReward(id)`** — เปลี่ยนสถานะภารกิจนั้นเป็น "รับรางวัลแล้ว" ตอนผู้ใช้กดปุ่มรับรางวัล
- **`readyToClaimCount()`** — นับว่ามีกี่ภารกิจที่ทำสำเร็จแล้วแต่ยังไม่ได้กดรับ (เอาไปโชว์เป็นตัวเลขแจ้งเตือน)
- **`handleClaim(id)`** (ใน `Achievements.jsx`) — เรียก `claimReward` แล้วโชว์ toast แจ้งว่า "รับรางวัลสำเร็จ"
- **การ์ดภารกิจแต่ละใบ** — คำนวณ progress bar จาก `current / target` และเปลี่ยนปุ่ม/สถานะไปตาม 3 สถานะ: ยังไม่ปลดล็อก (LOCKED), พร้อมรับ (READY_TO_CLAIM), รับแล้ว (CLAIMED)

---

## 4. ระบบวิดเจ็ต (Widgets)

### `src/pages/settings/widgetConstants.js` (ข้อมูลตั้งต้น)
- **`WIDGET_PLATFORMS`** — รายชื่อแพลตฟอร์มที่รองรับ (Facebook, Instagram, Discord) พร้อมสี/ไอคอน/ตัวเลือกพิเศษของแต่ละอัน
- **`createWidget(platformId, {url, options})`** — สร้างข้อมูลวิดเจ็ต 1 ชิ้นตามโครงสร้างมาตรฐาน
- **`getPlatformConfig(platformId)`** — ค้นหาข้อมูล (สี/ไอคอน/label) ของแพลตฟอร์มจาก id เพื่อเอาไปแสดงผล

### `src/pages/settings/SettingsWidgets.jsx` (หน้าจัดการวิดเจ็ต)
- **`persist(nextWidgets, message)`** — ส่งรายการวิดเจ็ตทั้งหมดไปบันทึกที่ backend ทีเดียว (ทับของเดิมทั้งก้อน)
- **`handleSaveWidget(platformId, {url, options})`** — เพิ่มวิดเจ็ตใหม่ หรือแก้ไขของเดิมถ้าแพลตฟอร์มนั้นมีอยู่แล้ว (1 แพลตฟอร์มมีได้แค่ 1 การ์ด)
- **`handleDeleteWidget(platformId)`** — ลบวิดเจ็ตของแพลตฟอร์มนั้นออก

### `src/components/settings/WidgetModal.jsx` (ป๊อปอัปเพิ่ม/แก้วิดเจ็ต)
- ตรวจสอบว่า URL ที่กรอกขึ้นต้นด้วย `http://` หรือ `https://` ก่อนให้บันทึกได้
- **`toggle(key, label, desc)`** — สร้างสวิตช์เปิด/ปิดตัวเลือกพิเศษของแต่ละแพลตฟอร์ม (เช่น "แสดงชื่อเพจ" ของ Facebook)

### `src/components/settings/WidgetCard.jsx` (การ์ดแสดงผล)
- ไม่มี logic ซับซ้อน แค่รับข้อมูลแพลตฟอร์ม+ตัวเลือกมาวาดเป็นการ์ดลิงก์สวยๆ ใช้ตัวเดียวกันซ้ำ 3 ที่ (พรีวิวในโมดัล, รายการในหน้าตั้งค่า, การ์ดจริงบนหน้าโปรไฟล์)

---

## ไฟล์ service กลางที่ทั้ง 4 ฟีเจอร์ใช้ร่วมกัน

**`src/services/profile.service.js`** — ฟังก์ชันเรียก API ที่ทุกหน้าด้านบนเรียกใช้ร่วมกัน:

- **`getMyPosts()` / `getDrafts()`** — ดึงโพสต์ของตัวเอง แล้วกรองว่าเป็นโพสต์เผยแพร่แล้วหรือแบบร่าง
- **`getUserProfile(userId)`** — ดึงข้อมูลโปรไฟล์ผู้ใช้ พร้อมจำนวนโพสต์/ผู้ติดตาม
- **`normalizeFollowCounts(profile)`** — แก้ตัวเลขผู้ติดตาม/กำลังติดตามที่ backend ส่งค่าสลับกันมา ให้ถูกต้องก่อนแสดงผล
- **`getMilestones()`** — ดึงรายการภารกิจ/รางวัลของ Achievements
- **`updateProfile(userId, data)`** — ฟังก์ชันกลางสำหรับ "บันทึก" ทุกอย่าง ไม่ว่าจะเป็นข้อมูลพื้นฐาน, ธีม, กรอบรูป, วอลเปเปอร์ หรือวิดเจ็ต — ทั้ง 4 ฟีเจอร์ด้านบนสุดท้ายแล้วเรียกฟังก์ชันนี้ตัวเดียวกันหมด
